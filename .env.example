# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# Default domain: https://sessaocerta.shop
APP_URL="https://sessaocerta.shop"

# TRANSACTIONAL EMAIL PROVIDER CONFIGURATION (RESEND)
RESEND_API_KEY=""
EMAIL_FROM="Sessão Certa <nao-responda@sessaocerta.shop>"

# SMTP / Alternative Fallbacks
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER="nao-responda@sessaocerta.shop"
SMTP_PASS=""
SMTP_FROM="Sessão Certa <nao-responda@sessaocerta.shop>"

